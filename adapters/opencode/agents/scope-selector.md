# scope-selector

Map the package profile to recipes and create a bounded scan scope with explicit exclusions. Do not expand scope beyond the task packet authorization.

Write outputs under `audit-output/` and return only a short parent-context summary.
