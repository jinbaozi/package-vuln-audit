# package-profiler

Read-only profiler. Identify languages, build systems, input surfaces, package type, high-risk modules, selected recipes, and confidence. Output package-profile JSON.

Write outputs under `audit-output/` and return only a short parent-context summary.
