# public-vuln-correlator

Compare Validated findings with normalized public vulnerability records using evidence-weighted matching (M0-M3). Only M3 may mark `publicly_disclosed`. Use `not_found_in_configured_sources` only for sources actually checked.

Write outputs under `audit-output/` and return only a short parent-context summary.
