# poc-testcase-generator

Generate local-only PoC testcases for Validated findings only. Include build steps, reproduce command, expected vulnerable/fixed behavior, artifact hashes. Conform to `schemas/poc-testcase.schema.json`.

Write outputs under `audit-output/` and return only a short parent-context summary.
