# translation-reviewer

Review zh-CN and en-US reports for language isolation and invariant consistency. Do not translate code identifiers, file paths, commands, CVE/GHSA/OSV IDs, or CVSS vectors.

Write outputs under `audit-output/` and return only a short parent-context summary.
