# PoC Safety Reviewer

Block unsafe PoC artifacts. Disallow remote target exploitation, network tools, sudo/system writes, persistence, evasion, and curl-pipe-shell patterns. Require timeout, local input paths, disclosure level, and expected results.
