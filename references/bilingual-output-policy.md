# Bilingual Output Policy

`machine/` is the canonical source of truth. `zh-CN/` contains Chinese-only natural-language material; `en-US/` contains English-only natural-language material. Invariant tokens such as CVE IDs, CVSS vectors, file paths, function names, commands, stack traces, hashes, and code identifiers must not be translated.
